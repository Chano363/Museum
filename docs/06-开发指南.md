# 开发指南

## 环境搭建

### 系统要求

- **操作系统:** Windows 10/11, macOS, Linux
- **Node.js:** >= 16.0.0
- **Python:** >= 3.9
- **Conda:** 推荐 (用于Python环境管理)

### 安装步骤

#### 1. 克隆项目

```bash
git clone <repository-url>
cd Museum
```

#### 2. 安装前端依赖

```bash
npm install
```

#### 3. 配置Python环境

**使用Conda (推荐):**

```bash
conda env create -f environment.yml
conda activate museum-env
```

**使用pip:**

```bash
pip install -r dynamic_gestures/requirements.txt
pip install flask flask-cors
```

#### 4. 下载模型文件

模型文件应放置在以下位置：

```
dynamic_gestures/models/
├── YOLOv10n_hands.onnx
├── crops_classifier.onnx
└── MobileNetV3_large.onnx

models/
└── hand_landmarker.task
```

MediaPipe模型会自动下载。

### 启动开发服务器

**同时启动前后端:**

```bash
npm run start
```

**单独启动:**

```bash
# 前端 (端口3002)
npm run dev

# 后端 (端口5000)
npm run backend
```

## 项目配置

### 前端配置 (vite.config.js)

```javascript
export default defineConfig({
  plugins: [vue()],
  server: {
    port: 3002,           // 前端端口
    open: true,           // 自动打开浏览器
    proxy: {
      '/api': {
        target: 'http://localhost:5000',  // 后端地址
        changeOrigin: true
      }
    }
  }
})
```

### 后端配置 (backend/app.py)

```python
# 模型配置
detection_model = HandDetection(
    'models/YOLOv10n_hands.onnx',
    image_size=(640, 640),
    confidence_threshold=0.4
)

# MediaPipe配置
options = vision.HandLandmarkerOptions(
    num_hands=1,
    min_hand_detection_confidence=0.5,
    min_hand_presence_confidence=0.5,
    min_tracking_confidence=0.5
)
```

### 手势识别配置 (src/constants/gestureConstants.ts)

```typescript
// 动作识别配置
ACTION_RECOGNITION_CONFIG = {
  MAX_HISTORY_LENGTH: 50,
  STABILITY_THRESHOLD: 0.6,
  MIN_HISTORY_LENGTH_STATIC: 5,
  MIN_HISTORY_LENGTH_DYNAMIC: 10
}

// 后端服务配置
BACKEND_CONFIG = {
  CONNECTION_TIMEOUT: 3000,
  MIN_REQUEST_INTERVAL: 100,
  IMAGE_WIDTH: 320,
  IMAGE_HEIGHT: 240
}

// 模型控制配置
MODEL_CONTROL_CONFIG = {
  ZOOM_STEP: 0.2,
  MIN_CAMERA_DISTANCE: 0.1,
  MAX_CAMERA_DISTANCE: 1
}
```

## 开发规范

### 代码风格

#### JavaScript/TypeScript

- 使用ES6+语法
- 组件使用Vue 3 Composition API 或 Options API
- 类型定义使用TypeScript

#### Python

- 遵循PEP 8规范
- 使用类型注解
- 函数和类添加docstring

### 组件开发

#### 组件结构

```vue
<template>
  <!-- 模板 -->
</template>

<script>
export default {
  name: 'ComponentName',
  components: { /* 子组件 */ },
  props: { /* 属性定义 */ },
  emits: { /* 事件定义 */ },
  data() { return { /* 响应式数据 */ } },
  computed: { /* 计算属性 */ },
  methods: { /* 方法 */ },
  mounted() { /* 生命周期 */ }
}
</script>

<style scoped>
/* 样式 */
</style>
```

#### 命名规范

- 组件文件: PascalCase (如 `MainView.vue`)
- 组件name: PascalCase
- props: camelCase
- emits: kebab-case
- CSS类: kebab-case

### 服务层开发

#### 服务类结构

```typescript
export class MyService {
  private config = { /* 配置 */ }
  private state = { /* 状态 */ }
  
  async initialize(): Promise<void> {
    // 初始化逻辑
  }
  
  async process(data: InputType): Promise<OutputType> {
    // 处理逻辑
  }
  
  reset(): void {
    // 重置状态
  }
}
```

## 添加新展品

### 1. 准备3D模型

将GLTF格式模型放入 `public/3Dmodels/` 目录：

```
public/3Dmodels/
└── 新展品名称/
    ├── scene.gltf
    ├── textures/
    │   └── *.jpeg
    └── ...
```

### 2. 添加展品数据

在 `src/App.vue` 的 `artifacts` 数组中添加：

```javascript
{
  id: 8,
  name: '新展品名称',
  dynasty: '朝代',
  description: '展品描述...',
  model: 'external',
  modelPath: '/3Dmodels/新展品名称/scene.gltf',
  iconPath: '/3Dmodels/新展品名称/textures/xxx.jpeg',
  color: '#颜色代码'
}
```

## 添加新手势

### 1. 更新类型定义

在 `src/types/gesture.ts` 中添加：

```typescript
export enum GestureType {
  // 现有手势...
  NEW_GESTURE = 45  // 新手势ID
}

export const GESTURE_NAMES: Record<number, string> = {
  // 现有映射...
  45: 'new_gesture'
}
```

### 2. 更新后端映射

在 `backend/app.py` 中添加：

```python
gesture_names = {
  # 现有映射...
  45: 'new_gesture'
}
```

### 3. 添加动作映射

在 `src/services/actionRecognition.ts` 中添加：

```typescript
const gestureToActionMap = {
  // 现有映射...
  45: 'new_action'
}
```

### 4. 实现动作处理

在 `src/components/MainView.vue` 中添加：

```javascript
onGestureAction(action) {
  switch (action) {
    // 现有处理...
    case 'new_action':
      this.handleNewAction()
      this.$emit('displayGestureHint', '新动作')
      break
  }
}
```

## 测试

### 运行测试

```bash
npm run test
```

### 测试文件结构

```
tests/
├── backend/           # 后端测试
│   ├── test_api.py
│   ├── test_backend.py
│   └── test_dynamic_gesture.py
├── frontend/          # 前端测试
│   ├── gesture_service.test.ts
│   └── components.test.ts
├── integration/       # 集成测试
│   └── test_end_to_end.py
└── photos/            # 测试图片
```

### 单元测试示例

**前端 (Jest):**

```typescript
describe('ActionRecognitionService', () => {
  let service: ActionRecognitionService
  
  beforeEach(() => {
    service = new ActionRecognitionService()
  })
  
  test('should return null when no detections', () => {
    const result = service.updateDetections([])
    expect(result).toBeNull()
  })
  
  test('should map gesture to action', () => {
    const detections = [{
      bbox: { x1: 0, y1: 0, x2: 100, y2: 100, confidence: 0.9 },
      gesture: 27,
      gestureName: 'like'
    }]
    
    // 需要多次调用以满足投票机制
    for (let i = 0; i < 10; i++) {
      service.updateDetections(detections)
    }
    
    const result = service.updateDetections(detections)
    expect(result).toBe('zoom_in')
  })
})
```

**后端 (pytest):**

```python
import pytest
from backend.app import app

@pytest.fixture
def client():
    app.config['TESTING'] = True
    with app.test_client() as client:
        yield client

def test_recognize_no_image(client):
    response = client.post('/api/recognize', json={})
    assert response.status_code == 400
```

## 调试技巧

### 前端调试

1. **Vue Devtools:** 安装Vue Devtools浏览器扩展
2. **控制台日志:** 开发模式下会输出详细日志
3. **导出日志:** 点击工具栏的📋按钮导出控制台日志

### 后端调试

1. **Flask调试模式:** 已启用debug=True
2. **打印日志:** 使用print()输出调试信息
3. **错误追踪:** API返回详细的traceback

### 手势识别调试

```typescript
// 在GestureControl.vue中添加
console.log('检测到手势:', gesture)
console.log('映射为动作:', action)
```

```python
# 在backend/app.py中添加
print(f'检测到 {len(boxes)} 个手部')
print(f'手势分类结果: {labels}')
```

## 常见问题

### 1. 模型加载失败

**问题:** 前端无法加载ONNX模型

**解决方案:**
- 检查模型文件路径是否正确
- 确保模型文件在 `public/models/` 目录
- 检查浏览器控制台错误信息

### 2. 后端连接失败

**问题:** 前端无法连接后端API

**解决方案:**
- 确认后端服务已启动 (端口5000)
- 检查Vite代理配置
- 检查防火墙设置

### 3. 手势识别不准确

**问题:** 手势识别结果不稳定

**解决方案:**
- 调整置信度阈值
- 增加投票历史长度
- 改善光照条件
- 确保手部在画面中清晰可见

### 4. 3D模型加载失败

**问题:** model-viewer无法加载模型

**解决方案:**
- 检查GLTF文件路径
- 确保模型文件完整
- 检查浏览器控制台错误
- 验证模型格式是否正确

## 性能优化

### 前端优化

1. **图像降分辨率**
   ```typescript
   IMAGE_WIDTH: 320
   IMAGE_HEIGHT: 240
   ```

2. **请求节流**
   ```typescript
   MIN_REQUEST_INTERVAL: 100
   ```

3. **组件懒加载**
   ```javascript
   const SettingView = () => import('./SettingView.vue')
   ```

### 后端优化

1. **GPU加速**
   ```python
   providers = ["CUDAExecutionProvider"]
   ```

2. **批量推理**
   ```python
   concatenated_crops = np.concatenate(crops, axis=0)
   ```

3. **内存优化**
   ```python
   options.enable_mem_pattern = False
   ```

## 部署

### 构建生产版本

```bash
npm run build
```

输出目录: `dist/`

### 后端部署

```bash
# 使用gunicorn (Linux)
gunicorn -w 4 -b 0.0.0.0:5000 backend.app:app

# 使用waitress (Windows)
waitress-serve --port=5000 backend.app:app
```

### Docker部署

```dockerfile
FROM node:18 AS frontend
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

FROM python:3.9
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
COPY --from=frontend /app/dist ./dist

CMD ["python", "backend/app.py"]
```

## 版本控制

### Git工作流

```bash
# 创建功能分支
git checkout -b feature/new-feature

# 提交更改
git add .
git commit -m "feat: 添加新功能"

# 合并到主分支
git checkout main
git merge feature/new-feature
```

### 提交信息规范

- `feat:` 新功能
- `fix:` 修复bug
- `docs:` 文档更新
- `style:` 代码格式
- `refactor:` 重构
- `test:` 测试
- `chore:` 构建/工具

## 相关资源

- [Vue 3 文档](https://vuejs.org/)
- [Vite 文档](https://vitejs.dev/)
- [Model Viewer 文档](https://modelviewer.dev/)
- [ONNX Runtime 文档](https://onnxruntime.ai/)
- [MediaPipe 文档](https://mediapipe.dev/)
- [Flask 文档](https://flask.palletsprojects.com/)
