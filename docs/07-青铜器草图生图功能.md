# 青铜器草图绘制 + AI生图功能技术文档

## 1. 功能概述

### 1.1 业务目标

为博物馆系统提供青铜器主题的交互式创作功能：
- 用户在画布上绘制草图轮廓
- 系统自动识别草图形状，调用AI生成青铜器纹理
- 若AI生成效果不佳，自动匹配预选纹理库中的相似模型

### 1.2 核心功能

| 功能模块 | 描述 |
|----------|------|
| 草图绘制 | Canvas画布，支持触控/鼠标绘制 |
| 形状识别 | 识别用户绘制的轮廓形状 |
| AI生图 | 基于草图+青铜器提示词生成纹理 |
| 纹理匹配 | AI失败时匹配预选纹理库 |
| 结果预览 | 将纹理贴到3D模型上展示 |

---

## 2. 技术架构

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                        前端 (Vue 3)                          │
├─────────────┬─────────────┬─────────────┬─────────────────┤
│ SketchPad   │ ShapeReco-  │ TexturePre- │ ModelViewer     │
│ 绘图画布    │ gnition     │ view        │ 3D预览          │
│             │ 形状识别     │ 纹理预览     │                 │
└──────┬──────┴──────┬──────┴──────┬──────┴────────┬────────┘
       │             │             │               │
       ▼             ▼             ▼               ▼
┌─────────────────────────────────────────────────────────────┐
│                    后端 (Flask)                              │
├─────────────┬─────────────┬─────────────┬─────────────────┤
│ /api/sketch │ /api/generate│ /api/match  │ /api/models     │
│ 草图上传    │ AI生图       │ 纹理匹配     │ 模型列表        │
└──────┬──────┴──────┬──────┴──────┬──────┴────────┬────────┘
       │             │             │               │
       ▼             ▼             ▼               ▼
┌─────────────────────────────────────────────────────────────┐
│                    AI服务层                                  │
├─────────────┬─────────────┬─────────────┬─────────────────┤
│ Stable      │ Shape       │ Texture     │ Bronze          │
│ Diffusion   │ Classifier  │ Matcher     │ Texture DB      │
│ 文生图模型   │ 形状分类器   │ 纹理匹配器   │ 青铜器纹理库     │
└─────────────┴─────────────┴─────────────┴─────────────────┘
```

### 2.2 技术选型

| 层级 | 技术 | 用途 |
|------|------|------|
| 前端绘图 | Canvas 2D API | 草图绘制 |
| 前端状态 | Vue 3 Composition API | 组件状态管理 |
| 形状识别 | 简化版轮廓分析 | 前端本地识别 |
| AI生图 | Stable Diffusion WebUI API | 后端调用 |
| 纹理匹配 | 特征向量相似度 | 后端处理 |
| 3D预览 | Three.js / model-viewer | 纹理贴图展示 |

---

## 3. 前端组件设计

### 3.1 组件结构

```
src/components/
├── SketchGenerator/
│   ├── SketchGenerator.vue      # 主容器组件
│   ├── SketchPad.vue            # 绘图画布
│   ├── ToolBar.vue              # 工具栏（画笔/橡皮/清除）
│   ├── GeneratePanel.vue        # 生成控制面板
│   └── TextureResult.vue        # 生成结果展示
```

### 3.2 SketchPad.vue 核心实现

```vue
<template>
  <div class="sketch-pad">
    <canvas 
      ref="canvasRef"
      @mousedown="startDrawing"
      @mousemove="draw"
      @mouseup="stopDrawing"
      @mouseleave="stopDrawing"
      @touchstart="handleTouchStart"
      @touchmove="handleTouchMove"
      @touchend="stopDrawing"
    />
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'

const canvasRef = ref<HTMLCanvasElement | null>(null)
let ctx: CanvasRenderingContext2D | null = null
let isDrawing = false
let lastX = 0
let lastY = 0

const props = defineProps({
  width: { type: Number, default: 400 },
  height: { type: Number, default: 400 },
  brushSize: { type: Number, default: 4 },
  brushColor: { type: String, default: '#ffffff' }
})

const emit = defineEmits<{
  (e: 'stroke', imageData: string): void
  (e: 'clear'): void
}>()

onMounted(() => {
  initCanvas()
})

function initCanvas() {
  const canvas = canvasRef.value
  if (!canvas) return
  
  canvas.width = props.width
  canvas.height = props.height
  ctx = canvas.getContext('2d')
  
  if (ctx) {
    ctx.fillStyle = '#1a1a1a'
    ctx.fillRect(0, 0, props.width, props.height)
    ctx.lineCap = 'round'
    ctx.lineJoin = 'round'
  }
}

function startDrawing(e: MouseEvent) {
  isDrawing = true
  const rect = canvasRef.value!.getBoundingClientRect()
  lastX = e.clientX - rect.left
  lastY = e.clientY - rect.top
}

function draw(e: MouseEvent) {
  if (!isDrawing || !ctx) return
  
  const rect = canvasRef.value!.getBoundingClientRect()
  const x = e.clientX - rect.left
  const y = e.clientY - rect.top
  
  ctx.beginPath()
  ctx.strokeStyle = props.brushColor
  ctx.lineWidth = props.brushSize
  ctx.moveTo(lastX, lastY)
  ctx.lineTo(x, y)
  ctx.stroke()
  
  lastX = x
  lastY = y
}

function stopDrawing() {
  if (isDrawing) {
    isDrawing = false
    emit('stroke', getImageData())
  }
}

function handleTouchStart(e: TouchEvent) {
  e.preventDefault()
  const touch = e.touches[0]
  const rect = canvasRef.value!.getBoundingClientRect()
  isDrawing = true
  lastX = touch.clientX - rect.left
  lastY = touch.clientY - rect.top
}

function handleTouchMove(e: TouchEvent) {
  e.preventDefault()
  if (!isDrawing || !ctx) return
  
  const touch = e.touches[0]
  const rect = canvasRef.value!.getBoundingClientRect()
  const x = touch.clientX - rect.left
  const y = touch.clientY - rect.top
  
  ctx.beginPath()
  ctx.strokeStyle = props.brushColor
  ctx.lineWidth = props.brushSize
  ctx.moveTo(lastX, lastY)
  ctx.lineTo(x, y)
  ctx.stroke()
  
  lastX = x
  lastY = y
}

function getImageData(): string {
  return canvasRef.value?.toDataURL('image/png') || ''
}

function clear() {
  if (!ctx) return
  ctx.fillStyle = '#1a1a1a'
  ctx.fillRect(0, 0, props.width, props.height)
  emit('clear')
}

defineExpose({ clear, getImageData })
</script>

<style scoped>
.sketch-pad {
  border: 2px solid var(--text-color, #C49210);
  border-radius: 8px;
  overflow: hidden;
}

canvas {
  display: block;
  cursor: crosshair;
  touch-action: none;
}
</style>
```

### 3.3 GeneratePanel.vue

```vue
<template>
  <div class="generate-panel">
    <div class="prompt-section">
      <label>青铜器类型</label>
      <select v-model="bronzeType">
        <option value="ding">鼎</option>
        <option value="zun">尊</option>
        <option value="jue">爵</option>
        <option value="gu">觚</option>
        <option value="pan">盘</option>
        <option value="you">卣</option>
        <option value="custom">自定义</option>
      </select>
    </div>
    
    <div class="style-section">
      <label>纹饰风格</label>
      <div class="style-options">
        <button 
          v-for="style in bronzeStyles" 
          :key="style.id"
          :class="{ active: selectedStyle === style.id }"
          @click="selectedStyle = style.id"
        >
          {{ style.name }}
        </button>
      </div>
    </div>
    
    <button 
      class="generate-btn"
      :disabled="isGenerating"
      @click="handleGenerate"
    >
      {{ isGenerating ? '生成中...' : '生成青铜器' }}
    </button>
  </div>
</template>

<script setup lang="ts">
import { ref, computed } from 'vue'

const bronzeType = ref('ding')
const selectedStyle = ref('taotie')

const bronzeStyles = [
  { id: 'taotie', name: '饕餮纹' },
  { id: 'yunlei', name: '云雷纹' },
  { id: 'kui', name: '夔龙纹' },
  { id: 'fengniao', name: '凤鸟纹' },
  { id: 'liuli', name: '蟠螭纹' }
]

const isGenerating = defineModel<boolean>('generating')

const emit = defineEmits<{
  (e: 'generate', config: GenerateConfig): void
}>()

interface GenerateConfig {
  bronzeType: string
  style: string
  prompt: string
}

const promptTemplate = computed(() => {
  const typeNames: Record<string, string> = {
    ding: '青铜鼎',
    zun: '青铜尊',
    jue: '青铜爵',
    gu: '青铜觚',
    pan: '青铜盘',
    you: '青铜卣'
  }
  
  const styleNames: Record<string, string> = {
    taotie: '饕餮纹',
    yunlei: '云雷纹',
    kui: '夔龙纹',
    fengniao: '凤鸟纹',
    liuli: '蟠螭纹'
  }
  
  return `中国古代${typeNames[bronzeType.value] || '青铜器'}，${styleNames[selectedStyle.value]}装饰，商周时期风格，青铜质感，古朴厚重，博物馆藏品，高清纹理`
})

function handleGenerate() {
  emit('generate', {
    bronzeType: bronzeType.value,
    style: selectedStyle.value,
    prompt: promptTemplate.value
  })
}
</script>
```

---

## 4. 后端API设计

### 4.1 API接口列表

| 接口 | 方法 | 描述 |
|------|------|------|
| `/api/sketch/upload` | POST | 上传草图图像 |
| `/api/generate` | POST | AI生成青铜器纹理 |
| `/api/generate/status` | GET | 查询生成状态 |
| `/api/textures/match` | POST | 匹配预选纹理库 |
| `/api/textures/list` | GET | 获取纹理列表 |

### 4.2 核心接口实现

```python
# backend/sketch_api.py

from flask import Blueprint, request, jsonify
import base64
import os
import requests
from typing import Optional
import numpy as np
from PIL import Image
import io

sketch_bp = Blueprint('sketch', __name__, url_prefix='/api')

# Stable Diffusion WebUI 配置
SD_WEBUI_URL = os.getenv('SD_WEBUI_URL', 'http://127.0.0.1:7860')

# 青铜器默认提示词
DEFAULT_NEGATIVE_PROMPT = """
low quality, blurry, distorted, modern, cartoon, 
anime, realistic photo, watermark, text, signature
"""

BRONZE_QUALITY_PROMPT = """
masterpiece, best quality, ancient Chinese bronze ware,
Shang Dynasty style, bronze texture, patina, 
museum collection, professional photography, 
detailed ornamentation, high resolution texture
"""


@sketch_bp.route('/generate', methods=['POST'])
def generate_texture():
    """
    AI生成青铜器纹理
    
    Request:
        {
            "sketch": "base64_image_data",
            "prompt": "青铜鼎，饕餮纹装饰...",
            "style": "taotie",
            "bronze_type": "ding",
            "strength": 0.75
        }
    
    Response:
        {
            "success": true,
            "texture_url": "/static/textures/generated/xxx.png",
            "fallback": false
        }
    """
    try:
        data = request.json
        sketch_data = data.get('sketch', '')
        user_prompt = data.get('prompt', '')
        style = data.get('style', 'taotie')
        bronze_type = data.get('bronze_type', 'ding')
        strength = data.get('strength', 0.75)
        
        # 构建完整提示词
        full_prompt = f"{user_prompt}, {BRONZE_QUALITY_PROMPT}"
        
        # 解码草图
        if sketch_data.startswith('data:image'):
            sketch_data = sketch_data.split(',')[1]
        
        sketch_bytes = base64.b64decode(sketch_data)
        
        # 调用 Stable Diffusion WebUI API
        result = call_sd_webui(
            sketch_bytes=sketch_bytes,
            prompt=full_prompt,
            negative_prompt=DEFAULT_NEGATIVE_PROMPT,
            strength=strength
        )
        
        if result['success']:
            return jsonify({
                'success': True,
                'texture_url': result['image_url'],
                'fallback': False
            })
        else:
            # AI生成失败，匹配预选纹理库
            fallback_result = match_texture_from_db(sketch_bytes, bronze_type, style)
            return jsonify({
                'success': True,
                'texture_url': fallback_result['texture_url'],
                'fallback': True,
                'matched_id': fallback_result['matched_id']
            })
            
    except Exception as e:
        return jsonify({
            'success': False,
            'error': str(e)
        }), 500


def call_sd_webui(sketch_bytes: bytes, prompt: str, negative_prompt: str, strength: float) -> dict:
    """
    调用 Stable Diffusion WebUI API 进行图生图
    """
    try:
        # 将草图转为base64
        sketch_base64 = base64.b64encode(sketch_bytes).decode('utf-8')
        
        payload = {
            "init_images": [f"data:image/png;base64,{sketch_base64}"],
            "prompt": prompt,
            "negative_prompt": negative_prompt,
            "denoising_strength": strength,
            "steps": 30,
            "cfg_scale": 7,
            "width": 512,
            "height": 512,
            "sampler_name": "DPM++ 2M Karras"
        }
        
        response = requests.post(
            f"{SD_WEBUI_URL}/sdapi/v1/img2img",
            json=payload,
            timeout=60
        )
        
        if response.status_code == 200:
            result = response.json()
            # 保存生成的图像
            image_data = result['images'][0]
            if image_data.startswith('data:image'):
                image_data = image_data.split(',')[1]
            
            # 生成唯一文件名
            import uuid
            filename = f"bronze_{uuid.uuid4().hex[:8]}.png"
            save_path = os.path.join('static', 'textures', 'generated', filename)
            
            os.makedirs(os.path.dirname(save_path), exist_ok=True)
            with open(save_path, 'wb') as f:
                f.write(base64.b64decode(image_data))
            
            return {
                'success': True,
                'image_url': f"/static/textures/generated/{filename}"
            }
        else:
            return {'success': False, 'error': 'SD WebUI request failed'}
            
    except requests.RequestException as e:
        return {'success': False, 'error': str(e)}


def match_texture_from_db(sketch_bytes: bytes, bronze_type: str, style: str) -> dict:
    """
    从预选纹理库匹配最相似的纹理
    """
    # 加载预选纹理库索引
    texture_db = load_texture_database()
    
    # 计算草图特征
    sketch_features = extract_sketch_features(sketch_bytes)
    
    # 筛选同类型的纹理
    candidates = [t for t in texture_db if t['bronze_type'] == bronze_type]
    
    if not candidates:
        candidates = texture_db
    
    # 计算相似度并排序
    best_match = None
    best_score = -1
    
    for texture in candidates:
        score = calculate_similarity(sketch_features, texture['features'])
        if score > best_score:
            best_score = score
            best_match = texture
    
    return {
        'texture_url': best_match['url'],
        'matched_id': best_match['id']
    }


def extract_sketch_features(image_bytes: bytes) -> np.ndarray:
    """
    提取图像特征向量（简化版：使用轮廓直方图）
    """
    image = Image.open(io.BytesIO(image_bytes)).convert('L')
    image = image.resize((256, 256))
    
    # 转为numpy数组
    arr = np.array(image)
    
    # 简单特征：直方图 + 边缘密度
    hist, _ = np.histogram(arr, bins=32, range=(0, 256))
    hist = hist / hist.sum()
    
    # 边缘检测
    from scipy import ndimage
    edges = ndimage.sobel(arr)
    edge_density = np.histogram(edges, bins=16)[0]
    edge_density = edge_density / edge_density.sum()
    
    return np.concatenate([hist, edge_density])


def load_texture_database() -> list:
    """
    加载预选纹理库配置
    """
    import json
    db_path = os.path.join('data', 'bronze_textures.json')
    
    if os.path.exists(db_path):
        with open(db_path, 'r', encoding='utf-8') as f:
            return json.load(f)
    
    return get_default_texture_db()


def get_default_texture_db() -> list:
    """
    默认青铜器纹理库（约50个预选纹理）
    """
    return [
        {
            "id": "bronze_ding_001",
            "url": "/static/textures/bronze/ding_001.png",
            "bronze_type": "ding",
            "style": "taotie",
            "name": "饕餮纹方鼎",
            "features": []  # 预计算的特征向量
        },
        {
            "id": "bronze_zun_001",
            "url": "/static/textures/bronze/zun_001.png",
            "bronze_type": "zun",
            "style": "fengniao",
            "name": "凤鸟纹尊",
            "features": []
        },
        # ... 更多预选纹理
    ]
```

### 4.3 注册蓝图

```python
# backend/app.py 中添加

from sketch_api import sketch_bp

app.register_blueprint(sketch_bp)
```

---

## 5. AI生图流程

### 5.1 完整流程图

```
用户绘制草图
      │
      ▼
┌─────────────┐
│ 前端收集    │
│ - 草图图像  │
│ - 青铜器类型│
│ - 纹饰风格  │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 构建提示词  │
│ 默认提示词  │
│ + 用户选择  │
└──────┬──────┘
       │
       ▼
┌─────────────┐     失败
│ 调用SD API  │────────────┐
│ 图生图      │            │
└──────┬──────┘            │
       │ 成功              │
       ▼                   ▼
┌─────────────┐     ┌─────────────┐
│ 返回生成结果│     │ 匹配预选库  │
│ fallback:   │     │ 计算相似度  │
│   false     │     │ 返回最佳匹配│
└──────┬──────┘     └──────┬──────┘
       │                   │
       └─────────┬─────────┘
                 │
                 ▼
         ┌─────────────┐
         │ 前端展示    │
         │ 纹理贴到3D  │
         │ 模型上预览  │
         └─────────────┘
```

### 5.2 提示词模板

```python
BRONZE_PROMPT_TEMPLATES = {
    "ding": {
        "base": "中国古代青铜鼎，三足两耳，",
        "styles": {
            "taotie": "饕餮纹装饰，兽面纹样，神秘威严",
            "yunlei": "云雷纹底纹，回旋几何图案",
            "kui": "夔龙纹饰，单足龙形纹样"
        }
    },
    "zun": {
        "base": "中国古代青铜尊，盛酒器，",
        "styles": {
            "fengniao": "凤鸟纹装饰，华丽精美",
            "taotie": "饕餮纹饰，庄重典雅"
        }
    },
    "jue": {
        "base": "中国古代青铜爵，饮酒器，三足流尾，",
        "styles": {
            "yunlei": "云雷纹装饰，简洁古朴"
        }
    }
}

def build_prompt(bronze_type: str, style: str) -> str:
    template = BRONZE_PROMPT_TEMPLATES.get(bronze_type, {})
    base = template.get("base", "中国古代青铜器，")
    style_desc = template.get("styles", {}).get(style, "")
    
    return f"""
{base}{style_desc}，
商周时期风格，青铜质感，铜绿锈迹，
古朴厚重，博物馆藏品级，
高清纹理，专业摄影，
masterpiece, best quality
"""
```

---

## 6. 预选纹理库设计

### 6.1 目录结构

```
public/
└── static/
    └── textures/
        ├── bronze/              # 预选青铜器纹理
        │   ├── ding/            # 鼎类
        │   │   ├── ding_001.png
        │   │   ├── ding_002.png
        │   │   └── ...
        │   ├── zun/             # 尊类
        │   ├── jue/             # 爵类
        │   └── ...
        └── generated/           # AI生成的纹理
            └── bronze_xxx.png

backend/
└── data/
    └── bronze_textures.json     # 纹理库索引
```

### 6.2 纹理库索引格式

```json
[
  {
    "id": "bronze_ding_001",
    "url": "/static/textures/bronze/ding/ding_001.png",
    "bronze_type": "ding",
    "style": "taotie",
    "name": "后母戊鼎风格",
    "period": "商代晚期",
    "features": [0.12, 0.34, 0.56, ...],
    "thumbnail": "/static/textures/bronze/ding/ding_001_thumb.png"
  },
  {
    "id": "bronze_ding_002",
    "url": "/static/textures/bronze/ding/ding_002.png",
    "bronze_type": "ding",
    "style": "yunlei",
    "name": "云雷纹圆鼎",
    "period": "西周",
    "features": [0.23, 0.45, 0.67, ...],
    "thumbnail": "/static/textures/bronze/ding/ding_002_thumb.png"
  }
]
```

### 6.3 预选纹理数量建议

| 青铜器类型 | 建议数量 | 主要纹饰风格 |
|------------|----------|--------------|
| 鼎 (ding) | 15-20 | 饕餮纹、云雷纹、夔龙纹 |
| 尊 (zun) | 10-15 | 凤鸟纹、饕餮纹 |
| 爵 (jue) | 8-10 | 云雷纹、简化纹 |
| 觚 (gu) | 8-10 | 饕餮纹、蕉叶纹 |
| 盘 (pan) | 5-8 | 蟠螭纹、鱼纹 |
| 卣 (you) | 5-8 | 凤鸟纹、夔龙纹 |
| **总计** | **~50-70** | |

---

## 7. 前端服务层

### 7.1 sketchGenerator.ts

```typescript
// src/services/sketchGenerator.ts

import { ref, type Ref } from 'vue'

const API_BASE = import.meta.env.VITE_API_BASE || ''

export interface GenerateConfig {
  bronzeType: string
  style: string
  prompt: string
}

export interface GenerateResult {
  success: boolean
  textureUrl: string
  fallback: boolean
  matchedId?: string
  error?: string
}

export class SketchGeneratorService {
  private isGenerating: Ref<boolean> = ref(false)
  
  get generating() {
    return this.isGenerating
  }
  
  async generateFromSketch(
    sketchDataUrl: string,
    config: GenerateConfig
  ): Promise<GenerateResult> {
    this.isGenerating.value = true
    
    try {
      const response = await fetch(`${API_BASE}/api/generate`, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json'
        },
        body: JSON.stringify({
          sketch: sketchDataUrl,
          prompt: config.prompt,
          style: config.style,
          bronze_type: config.bronzeType,
          strength: 0.75
        })
      })
      
      if (!response.ok) {
        throw new Error(`HTTP error: ${response.status}`)
      }
      
      const result = await response.json()
      return result
      
    } catch (error) {
      console.error('Generate failed:', error)
      return {
        success: false,
        textureUrl: '',
        fallback: false,
        error: error instanceof Error ? error.message : 'Unknown error'
      }
    } finally {
      this.isGenerating.value = false
    }
  }
  
  async getTextureList(bronzeType?: string): Promise<any[]> {
    const params = new URLSearchParams()
    if (bronzeType) {
      params.append('type', bronzeType)
    }
    
    const response = await fetch(
      `${API_BASE}/api/textures/list?${params.toString()}`
    )
    
    if (!response.ok) {
      return []
    }
    
    return response.json()
  }
}

export const sketchGenerator = new SketchGeneratorService()
```

---

## 8. 快速实现步骤

### Phase 1: 基础绘图功能 (1-2天)

1. 创建 `SketchPad.vue` 组件
2. 实现基础Canvas绘图（画笔、清除）
3. 支持触控和鼠标操作
4. 添加工具栏（画笔大小、颜色）

### Phase 2: 后端API (1-2天)

1. 创建 `sketch_api.py` 蓝图
2. 实现 `/api/generate` 接口框架
3. 配置 Stable Diffusion WebUI 连接
4. 实现图生图调用

### Phase 3: 预选纹理库 (1天)

1. 准备50+青铜器纹理图片
2. 创建纹理库索引JSON
3. 实现特征提取和相似度匹配
4. 实现fallback机制

### Phase 4: 集成测试 (1天)

1. 前后端联调
2. 3D模型纹理贴图
3. 错误处理和用户反馈
4. 性能优化

---

## 9. 依赖安装

### 前端

无需额外依赖，使用浏览器原生Canvas API。

### 后端

```bash
# 激活conda环境
conda activate dynamic

# 安装依赖
pip install pillow scipy numpy

# Stable Diffusion WebUI (独立部署)
# 参考: https://github.com/AUTOMATIC1111/stable-diffusion-webui
```

### Stable Diffusion WebUI 配置

```bash
# 启动时开启API
./webui.sh --api --listen

# 或在 Windows
webui.bat --api --listen
```

---

## 10. 注意事项

1. **AI生图超时**: 设置60秒超时，超时后自动使用预选纹理库
2. **图像尺寸**: 统一使用512x512，平衡质量和性能
3. **提示词安全**: 过滤用户输入，防止注入攻击
4. **纹理缓存**: 生成的纹理本地缓存，避免重复生成
5. **移动端适配**: 触控事件需要阻止默认行为

---

## 11. 环境变量配置

AI 生图功能使用即梦 AI 服务，需要配置以下环境变量：

> **注意**: 即梦账号需要自己注册，注册后会赠送 200 次生图次数。
> 注册地址: https://console.volcengine.com/ark

| 变量名 | 必填 | 默认值 | 说明 |
|--------|------|--------|------|
| `AI_PROVIDER` | 是 | jimeng | AI 生图提供商 |
| `ARK_API_KEY` | 是 | - | 火山引擎 Ark API Key |
| `JIMENG_MODEL` | 否 | doubao-seedream-4-5-251128 | 即梦 AI 模型名称 |

### 配置示例

```env
# .env 文件配置
AI_PROVIDER=jimeng
ARK_API_KEY=你的API密钥
JIMENG_MODEL=doubao-seedream-4-5-251128
```

### API Key 获取方式

1. 访问 https://console.volcengine.com/ark
2. 注册账号（赠送 200 次生图次数）
3. 创建 API Key
4. 开通 doubao-seedream 模型

---

## 12. 使用示例

### 前端调用示例

```typescript
import { sketchGenerator } from '@/services/sketchGenerator'

// 生成青铜器纹理
async function generateBronzeTexture(sketchDataUrl: string) {
  const result = await sketchGenerator.generateFromSketch(sketchDataUrl, {
    bronzeType: 'ding',      // 鼎
    style: 'taotie',         // 饕餮纹
    prompt: '中国古代青铜鼎，饕餮纹装饰，商周时期风格'
  })
  
  if (result.success) {
    console.log('纹理URL:', result.textureUrl)
    console.log('是否使用预选库:', result.fallback)
  }
}
```

### 后端 API 调用示例

```python
import requests
import base64

# 读取草图图片
with open('sketch.png', 'rb') as f:
    sketch_base64 = base64.b64encode(f.read()).decode()

# 调用生成接口
response = requests.post('http://localhost:5000/api/generate', json={
    'sketch': f'data:image/png;base64,{sketch_base64}',
    'prompt': '中国古代青铜鼎，饕餮纹装饰',
    'style': 'taotie',
    'bronze_type': 'ding',
    'strength': 0.75
})

result = response.json()
if result['success']:
    print(f"生成成功: {result['texture_url']}")
    if result.get('fallback'):
        print("使用了预选纹理库")
```

---

## 13. 常见问题

### Q1: AI 生图失败怎么办？

**A:** 
1. 检查 API Key 是否正确
2. 确认账户余额充足
3. 查看后端日志获取详细错误信息
4. 系统会自动回退到预选纹理库

### Q2: 生成速度慢怎么办？

**A:** 
1. 使用本地 Stable Diffusion WebUI（需要 GPU）
2. 降低生成步数
3. 使用更小的图像尺寸

### Q3: 如何添加新的青铜器类型？

**A:** 
1. 在 `GeneratePanel.vue` 中添加选项
2. 在 `BRONZE_PROMPT_TEMPLATES` 中添加提示词模板
3. 准备对应的预选纹理

### Q4: 生成的纹理质量不好怎么办？

**A:** 
1. 调整 `strength` 参数（0.5-0.9）
2. 优化提示词描述
3. 使用更高质量的草图
